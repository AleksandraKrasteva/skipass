/* eslint-disable linebreak-style */
/** @type {import('next').NextConfig} */

const nextConfig = {
  output: "standalone",
  serverRuntimeConfig: {
    runtime: "edge",
  },
};

export default nextConfig;
